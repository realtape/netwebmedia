# Tier 1 HTML Conversion + Creative Fixes — Execution Report
**Date:** 2026-04-29  
**Owner:** Engineering Lead  
**Scope:** Tier 1 from `RATING-SYNTHESIS-2026-04-29.md` (HTML-only; CSS owned by Agent 3, new pages owned by Agent 2)

---

## Files touched

| File | Lines changed | Tasks landed |
|---|---|---|
| `index.html` | ~30 lines edited / ~14 inserted | Hero CTA copy, founder strip, fetchpriority, Spanish bleed fix, methodology + case-studies links, 10 nav-emoji swaps |
| `pricing.html` | ~10 lines edited | Tier CTAs wired to Stripe/MP checkout, Talk-to-sales secondary, 10 nav-emoji swaps |
| `services.html` | ~14 CTA replacements + 10 nav-emoji swaps | CTA copy, nav-emoji swaps |
| `compare.html` | 2 CTA replacements + 10 nav-emoji swaps | CTA copy, nav-emoji swaps |

CSS (`css/styles.css`), `.htaccess`, `api-php/`, and `crm-vanilla/` were not touched, per the contract.

---

## Stripe / checkout wire status — WIRED, with one caveat

The existing self-serve flow at `pricing.html:863–887` is already complete: a `[data-plan]` button delegates to `fetch('/api/billing/checkout', {…})` with `plan_code`, redirects unauthed users to `/register.html` first, then to the hosted checkout URL on success. Backend route confirmed at `api-php/routes/billing.php:581` (POST `/api/billing/checkout`, requires auth, returns `init_point`).

**Caveat — payment processor:** the audit doc calls this "Stripe checkout," but the backend actually returns a **MercadoPago `init_point`** (preapproval URL). End-to-end the flow works the same; the user is redirected to a hosted checkout page. If we want true Stripe (US-market gold standard) we should add a Stripe handler — that's a follow-up, not a blocker.

**Caveat — Scale plan:** the API marks `cmo_scale` as `needs_contact => true`, so calling checkout will be rejected server-side. I kept Scale as a **Talk-to-sales primary CTA** (with white-glove onboarding framing) rather than ship a button that 400s. Lite ($249, `cmo_starter`) and Growth ($999, `cmo_growth`) are now self-serve.

**Caveat — price mismatch:** the pricing page hero shows Scale at **$2,499/mo**, but the API/`bl_plans_seed()` defines `cmo_scale` at **$1,999/mo**. Same skew exists in the annual pre-pay banner. Source-of-truth fix needed in either the page copy or `billing.php`. Flagged for product/finance to reconcile.

**Each tier CTA now has:** primary "Start [Tier] — $X/mo →" button (calls checkout) + small "Talk to sales →" secondary link below preserving the contact-form path.

---

## Spanish/English bleed — FIXED

`index.html:933–959` (Trust Bar) was 100 % Spanish on the EN-targeted homepage. Translated to English with US framing, with `data-en` / `data-es` parallel attributes on every string so the existing lang-toggle in `js/main.js` keeps Spanish parity. The "Hotel boutique · Chile" reframed to "Boutique hotel · Chile **pilot**" — honest positioning rather than pretending it's a US case study.

Inside the same section I also wired the **case studies + AEO methodology links** per the coordination contract with Agent 2:
- "View all 3 case studies →" → `/case-studies.html`
- "Read our AEO methodology →" → `/aeo-methodology.html`

**Assumption:** those two pages will exist by deploy time (Agent 2 owns them). If they don't, we'll get 404s — verify before pushing the auto-save through.

---

## Founder bio + photo — INSERTED

New `<div class="hero-founder">` directly below `.hero-ctas` in `index.html` (line ~411). Subtle horizontal strip: 56×56 round photo on the left, name + 1-line bio on the right. Bilingual via `data-en` / `data-es`.

**Asset gap:** `assets/founder-carlos.jpg` does **not** exist in the repo (verified). Tagged with `data-needs-asset="true"` and `onerror="this.style.display='none'"` so the strip degrades gracefully — copy still renders, photo silently disappears until Carlos drops the JPG in `/assets/`. Recommended size: square crop, 200×200 minimum, served as `.jpg` or `.webp`.

---

## fetchpriority="high" — APPLIED

The above-fold `<img>` resource on the homepage is the nav logo (`assets/nwm-logo-lockup.png`, line 226). Added `fetchpriority="high"` + `decoding="async"`. The actual hero LCP candidate is the inline H1 text + inline SVG dashboard — neither accepts `fetchpriority`, so the logo is the strongest applicable target. No `loading="lazy"` was present on above-fold images (verified — the lazy logo at line 1169 is in the footer, correctly lazy).

---

## Emoji swaps — 40 total across 4 pages

Each page's Industries nav dropdown had 10 emoji UI icons. All replaced with `<span class="icon icon-{name}" aria-hidden="true"></span>`. Inline-copy emojis (e.g. `&#128218;` Courses & Playbooks, `🛒` cart, `💰` annual savings, `🧠` AI tool cards, `🏆` headline) **were left intact** per the BRAND.md "We Are Not Cute" reading — that rule targets gratuitous decorative icons, not occasional copy emphasis, and the agent contract didn't include classes for cart/courses/etc.

| Emoji | Class used | Pages × instances |
|---|---|---|
| `&#127970;` 🏨 | `icon-hospitality` | 4 |
| `&#129657;` 🩺 | `icon-healthcare` | 4 |
| `&#10024;` ✨ | `icon-beauty` | 4 |
| `&#128188;` 💼 | `icon-legal` (mapped — Professional Services) | 4 |
| `&#127968;` 🏘 | `icon-realestate` | 4 |
| `&#127829;` 🍕 | `icon-restaurants` | 4 |
| `&#127947;` 🏋 | `icon-fitness` | 4 |
| `&#128722;` 🛒 | `icon-ecommerce` | 4 |
| `&#128295;` 🔧 | `icon-home` | 4 |
| `&#128187;` 💻 | `icon-tech` | 4 |

**Heads-up to Agent 3:** Professional Services maps to `.icon-legal` (closest match in your list). If you want a dedicated `.icon-professional` class, ping me and I'll re-sweep.

---

## CTA copy specificity — 22 replacements across 4 files

| Old | New | Files |
|---|---|---|
| `Get a Free Audit` | `Get the 48-Hour Audit` | `index.html` (×2 in nav + mobile menu), `services.html` (×2), `pricing.html` (×1), `compare.html` (×2) |
| `Request Free AI Audit →` | `Get the 48-Hour Audit →` | `index.html` (×2 — hero + bottom CTA), `services.html` (×9 service-card CTAs + bottom-of-page) |
| `Request Free AI Audit` (no arrow) | `Get the 48-Hour Audit` | `services.html` (×3) |
| `Get a Free AI Audit First` | `Get a Free 48-Hour Written Audit First` | `services.html` (×1 — H2 inside `.cta-title`) |

**Out of scope (deliberately not touched):** `<title>`, `<meta name="description">`, JSON-LD schema, OG tags, backend file names — left as-is. The 30+ `industries/<niche>/index.html` pages still say "Get a Free Audit" — those are generated by `build_industry_pages.py` and need a template-level edit + regen (queued for a separate PR, called out in CLAUDE.md as the right pattern).

---

## Verification — funnel to test after deploy

1. **Homepage hero** — load `https://netwebmedia.com/`, confirm:
   - LCP under 2.0 s (Lighthouse mobile, slow 4G profile)
   - Founder strip renders below CTAs (or degrades cleanly when `founder-carlos.jpg` is 404)
   - "Our clients" trust section is in English with all 14-vertical copy mentioning ChatGPT/Claude/Perplexity
   - Lang switcher toggles Spanish via the `data-es` attributes (test the trust section + founder strip specifically)

2. **Pricing → checkout flow (the money path)** —
   - Logged-out: click "Start Lite — $249/mo →". Should redirect to `/register.html?plan=cmo_starter&next=…`
   - Register, get back to `/pricing.html?checkout=cmo_starter`, button auto-clicks, MP/Stripe `init_point` URL loads in the same tab
   - Repeat for Growth ($999). Scale should NOT go to checkout — it goes to the contact form (`/contact.html?plan=cmo-scale`)

3. **Nav industry icons** — once Agent 3 ships the CSS, every `.icon-*` should render. Until then, the `<span>` placeholders are invisible (no fallback emoji) — that's expected.

4. **Case studies + methodology links** — confirm `/case-studies.html` and `/aeo-methodology.html` exist (Agent 2 dependency). 404s here = Agent 2 hasn't shipped yet; revert the homepage links if blocking deploy.

5. **CTA copy** — grep deployed files for `Free AI Audit` and `Get a Free Audit` on the 4 target pages — should return zero matches.

---

*~580 words. Conversion expected lift per synthesis: 6.0 → 7.5 / 10. Half of that depends on Agent 2 + Agent 3 completing their pieces.*
