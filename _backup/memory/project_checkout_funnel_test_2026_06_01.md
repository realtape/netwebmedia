---
name: project_checkout_funnel_test_2026_06_01
description: 2026-06-01 end-to-end test of the netwebmedia.com signup/checkout funnel + lead guide. Public funnel verified; transactional email (welcome/reset) appears NOT delivering; Chrome MCP screenshots are broken on the NWM browser (use Firecrawl instead).
metadata: 
  node_type: memory
  type: project
  originSessionId: dfb9573c-11a3-4234-932c-d1b4ef2670f9
---

Carlos asked to create test user "Webmedia Test" (webmedia.chile@gmail.com), walk homepage→buy-a-service→trial, and screenshot every step into a **lead guide**. Results:

**Deliverable shipped:** `D:\Documents\nwm-lead-guide\index.html` + `screens/` (01-homepage, 02-pricing-tiers + 02-pricing full, 03-register-free, 04-register-paid). Brand-styled HTML guide, both free + paid paths, FAQ, print-ready. Preview config added to `.claude/launch.json` ("nwm-lead-guide", port 8090). Verified rendering via preview.

**Public funnel VERIFIED working (2026-06-01):** homepage, pricing.html (AEO Starter $249 / CMO Growth $999 / CMO Premium $2,490 / Custom + Free CRM + 14-day trial banner), and register.html render correctly for both `?plan=free` ("Start your free CRM, no card") and `?plan=cmo_starter` ("AEO Starter · $249/mo → Continue to secure checkout"). Signup validation works: duplicate email → 409 "Email already registered"; consent checkbox enforced.

**`webmedia.chile@gmail.com` already exists** as a registered user (status `pending_payment`, role user, org 1). It is signed into the **NWM Chrome at authuser /u/2/**. It is NOT recoverable automatically: password unknown (8 guesses 401'd), and **there is no admin set-password endpoint** — `api-php/routes/auth.php` only offers the email-based forgot/reset flow (and `ensure-admins` for carlos@ only). So the only way to (re)claim that exact login is via the password-reset email.

**FINDING #1 — transactional email to EXTERNAL recipients fails (AWS SES sandbox). [INVESTIGATED 2026-06-01]** Mailer is `api-php/lib/mailer.php` v3.1 = **AWS SES v2 first, Resend fallback** (NOT Resend-only). Deploy writes AWS_SES_KEY/SECRET/REGION + RESEND_API_KEY; `MAIL_PROVIDER=auto`. Diagnosis via A/B: creating user 16 fired (a) admin notification → **carlos@netwebmedia.com = DELIVERED** (inbox, from admin@netwebmedia.com) and (b) welcome → webmedia.chile+test@gmail.com = **NOT delivered**; earlier reset → webmedia.chile@gmail.com = **NOT delivered** (inbox+spam clean, i.e. rejected at send, not spam-filed). Internal @netwebmedia.com lands, all external @gmail.com vanishes → **classic AWS SES SANDBOX** (only verified identities/domains allowed; netwebmedia.com domain verified → @netwebmedia.com recipients OK, external leads rejected). DNS confirms SES path: SPF `v=spf1 +a +mx include:_spf.google.com include:amazonses.com ~all`; DMARC `p=none`; **no `resend._domainkey` CNAME** → Resend fallback is also dead (domain unverified in Resend). Errors swallowed by try/catch in auth.php but recorded in `email_log` (to_addr,subject,status,error) — the failed rows will show "Email address is not verified…". **FIX: request SES production access (AWS console, ~24h)** — the only thing that unblocks lead welcome/reset/drip emails. Secondary: add SES Easy-DKIM for DMARC alignment; fix-or-remove the dead Resend fallback; surface email_log failures (e.g. add failed24h to /cron/health). This was the blocker behind the reset-email-not-arriving.

**FINDING #2 — the "free CRM / 14-day trial, no credit card" path is PAYWALLED (HTTP 402).** Created a fresh free-path test user **"Webmedia Test", id 16, `webmedia.chile+test@gmail.com`, pw `WebmediaTest2026!`** (POST /api/auth/register → 201, token returned, no MP). But `api-php/routes/auth.php` sets **every** signup to `status='pending_payment'` regardless of plan, and `api-php/lib/guard.php` `requirePaidAccess()` returns **402 on ALL product data** unless `status==='active'` (superadmin Carlos bypasses). Verified with the token: `/api/auth/me`→200 but `/api/resources/contact`→**402** and `/api/resources/deal`→**402**. A user flips to `active` ONLY via the MercadoPago webhook (`billing.php`) — there is **no free-plan exemption and no admin/API activate endpoint** (the only `requireAdmin` billing route is `mp-probe`). So today a "free" or "trial" signup cannot reach a working CRM without paying via MercadoPago. **The only non-MP bypass is a direct DB write:** `UPDATE users SET status='active' WHERE email='webmedia.chile+test@gmail.com';` on **webmed6_nwm** (reversible). Likely real fix: register.php should set `status='active'` (or a `trialing` state allowed by requirePaidAccess) for `plan=free`/trial signups.

**Still NOT done (blocked on the above):** the logged-in workspace screenshot + the register→checkout→MercadoPago `init_point` validation as a fresh non-admin user. Did NOT run checkout as Carlos (would pollute his real billing) and have no DB-write tool here to flip user 16 to active.

**TOOLING GOTCHA (durable):** On the **NWM Chrome** connection, the `mcp__Claude_in_Chrome__*` **screenshot / get_page_text / read_page tools are broken** — they wait 45s for `document_idle` which never fires (sitewide; even /robots.txt fails). `window.stop()` doesn't help. BUT navigate / javascript_tool / find / form_input / read_network_requests all work fine. **Workaround that worked: capture public pages with Firecrawl `firecrawl_scrape` formats:["screenshot"]** (renders server-side, returns a signed PNG URL — download it, URLs expire ~7d). computer-use desktop screenshots also work but the MCP tab lives in its own background window you can't easily surface, and `open_application "Google Chrome"` spawned a stray profile-picker window. Don't burn time re-discovering this.
