<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=1080, height=1920" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800;900&family=Poppins:wght@400;600;700;800;900&display=swap"
      rel="stylesheet"
    />
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&display=swap" rel="stylesheet"/>
    <script src="https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/gsap.min.js"></script>
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      html, body {
        width: 1080px; height: 1920px; overflow: hidden;
        background: #010F3B; font-family: 'Poppins', 'Inter', sans-serif;
      }

      .clip { position: absolute; top: 0; left: 0; }
      .scene {
        width: 1080px; height: 1920px;
        display: flex; flex-direction: column;
        align-items: center; justify-content: center;
        padding: 80px;
      }

      /* Fix 1: PNG logo card — note: index.html uses assets/ not ../assets/ */
      .nwm-logo { position: absolute; top: 40px; right: 40px; z-index: 200;
        background: #FFFFFF; padding: 10px 18px; border-radius: 10px;
        box-shadow: 0 4px 18px rgba(0,0,0,0.15); opacity: 0.98; }
      .nwm-logo img { display: block; width: 220px; height: auto; }
      .nwm-logo-cta { position: absolute; bottom: 90px; left: 50%; transform: translateX(-50%);
        z-index: 10; opacity: 0; background: #FFFFFF; padding: 14px 24px;
        border-radius: 12px; box-shadow: 0 4px 18px rgba(0,0,0,0.15);
        width: 328px; height: 72px;
        background-image: url(assets/nwm-logo.png);
        background-size: 300px auto; background-repeat: no-repeat; background-position: center; }
      .nwm-logo-cta img { display: block; width: 300px; height: auto; }

      /* Fix 3: B-roll layers */
      .broll { position: absolute; top: 0; left: 0; width: 1080px; height: 1920px; object-fit: cover; }
      .broll-tint { position: absolute; top: 0; left: 0; width: 1080px; height: 1920px;
        background: linear-gradient(180deg, rgba(1,15,59,0.82) 0%, rgba(1,15,59,0.92) 100%); }

      /* Scene 1: Hook */
      #scene-hook { background: transparent; }
      #hook-overline { color: #FF671F; font-size: 20px; font-weight: 700; letter-spacing: 4px; text-transform: uppercase; opacity: 0; margin-bottom: 28px; }
      #hook-headline { color: #FFFFFF; font-size: 88px; font-weight: 900; line-height: 1.05; text-align: center; text-transform: uppercase; opacity: 0; transform: translateY(50px); }
      #hook-headline .accent { color: #FF671F; }
      #hook-bar { width: 0px; height: 6px; background: #FF671F; border-radius: 3px; margin: 44px 0; opacity: 0; }
      #hook-sub { color: rgba(255,255,255,0.65); font-size: 30px; font-weight: 400; text-align: center; line-height: 1.5; max-width: 860px; opacity: 0; }

      /* Scene 2: Stat */
      #scene-stat { background: transparent; position: relative; overflow: hidden; }
      #stat-ring { position: absolute; width: 740px; height: 740px; border-radius: 50%; border: 2px solid rgba(255,103,31,0.18); top: 50%; left: 50%; transform: translate(-50%,-50%) scale(0.4); opacity: 0; }
      #stat-ring-2 { position: absolute; width: 560px; height: 560px; border-radius: 50%; border: 2px solid rgba(255,103,31,0.10); top: 50%; left: 50%; transform: translate(-50%,-50%) scale(0.4); opacity: 0; }
      #stat-inner { position: relative; z-index: 2; display: flex; flex-direction: column; align-items: center; text-align: center; }
      #stat-big { color: #FF671F; font-size: 180px; font-weight: 900; line-height: 1; opacity: 0; }
      #stat-label { color: #FFFFFF; font-size: 44px; font-weight: 700; margin-top: 16px; opacity: 0; max-width: 860px; line-height: 1.25; }
      #stat-detail { color: rgba(255,255,255,0.55); font-size: 26px; font-weight: 400; margin-top: 20px; opacity: 0; max-width: 820px; line-height: 1.5; }

      /* Scene 3: CTA — orange bg, no b-roll */
      #scene-cta { background: #FF671F; }
      #cta-badge { background: #010F3B; color: #FF671F; font-size: 18px; font-weight: 700; letter-spacing: 3px; text-transform: uppercase; padding: 10px 24px; border-radius: 50px; opacity: 0; margin-bottom: 36px; }
      #cta-headline { color: #010F3B; font-size: 62px; font-weight: 900; text-align: center; line-height: 1.1; max-width: 900px; opacity: 0; }
      #cta-btn { background: #010F3B; color: #FFFFFF; font-family: 'Poppins', sans-serif; font-size: 30px; font-weight: 700; padding: 24px 56px; border-radius: 50px; margin-top: 60px; opacity: 0; letter-spacing: 0.3px; }
      #cta-link { color: rgba(1,15,59,0.65); font-size: 22px; font-weight: 500; margin-top: 22px; opacity: 0; }
    </style>
  </head>
  <body>

    <div
      id="root"
      data-composition-id="main"
      data-start="0"
      data-duration="14"
      data-width="1080"
      data-height="1920"
    >

      <!-- Fix 1: PNG logo card, persistent across all scenes, track-index 10 -->
      <div class="nwm-logo clip" data-start="0" data-duration="14" data-track-index="10">
        <img src="assets/nwm-logo.png" alt="netwebmedia"/>
      </div>

      <!-- Scene 1: Hook 0–5s — b-roll -->
      <video id="broll-main-s1" class="broll clip" muted autoplay loop
             src="assets/broll/mk-5254.mp4"
             data-start="0" data-duration="5" data-track-index="0"></video>
      <div class="broll-tint clip" data-start="0" data-duration="5" data-track-index="1"></div>
      <div
        id="scene-hook"
        class="clip scene"
        data-start="0"
        data-duration="5"
        data-track-index="2"
      >
        <div id="hook-overline">NetWebMedia</div>
        <div id="hook-headline">
          <span class="accent">1.1&times;</span> TO<br>
          <span class="accent">3.5&times;</span><br>
          IN 47 DAYS.
        </div>
        <div id="hook-bar"></div>
        <div id="hook-sub">
          Here's the exact playbook that took a DTC brand<br>
          from losing money on Meta to printing it.
        </div>
      </div>

      <!-- Scene 2: Stat 5–10s — b-roll -->
      <video id="broll-main-s2" class="broll clip" muted autoplay loop
             src="assets/broll/mk-5254.mp4"
             data-start="5" data-duration="5" data-track-index="0"></video>
      <div class="broll-tint clip" data-start="5" data-duration="5" data-track-index="1"></div>
      <div
        id="scene-stat"
        class="clip scene"
        data-start="5"
        data-duration="5"
        data-track-index="2"
      >
        <div id="stat-ring"></div>
        <div id="stat-ring-2"></div>
        <div id="stat-inner">
          <div id="stat-big">3.5&times;</div>
          <div id="stat-label">Meta ROAS — 47 Days</div>
          <div id="stat-detail">
            Same ad spend. +$190K monthly net profit.<br>
            DTC brand &middot; $4M ARR &middot; 3 levers rebuilt.
          </div>
        </div>
      </div>

      <!-- Scene 3: CTA 10–14s — orange bg, no b-roll -->
      <div
        id="scene-cta"
        class="clip scene"
        data-start="10"
        data-duration="4"
        data-track-index="1"
      >
        <div id="cta-badge">Fractional CMO</div>
        <div id="cta-headline">Ready to 3&times; your ROAS this quarter?</div>
        <div id="cta-btn">Book a Discovery Call</div>
        <div id="cta-link">&#8593; Link in bio</div>
      </div>
      <!-- Fix 1: CTA logo card -->
      <div class="nwm-logo-cta clip" data-start="10" data-duration="4" data-track-index="11"></div>

    </div>

    <script>
      window.__timelines = window.__timelines || {};
      const tl = gsap.timeline({ paused: true });

      /* Scene 1: Hook (t = 0–5s) */
      tl.to("#hook-overline",  { opacity: 1, duration: 0.4 }, 0.2)
        .to("#hook-headline",  { opacity: 1, y: 0, duration: 0.7, ease: "power3.out" }, 0.5)
        .to("#hook-bar",       { opacity: 1, width: 90, duration: 0.5, ease: "power2.out" }, 1.3)
        .to("#hook-sub",       { opacity: 1, duration: 0.6 }, 1.6);

      /* Scene 2: Stat (t = 5–10s) */
      tl.to(["#stat-ring","#stat-ring-2"],
                               { scale: 1, opacity: 1, duration: 0.9, ease: "back.out(1.4)", stagger: 0.15 }, 5.2)
        .to("#stat-big",       { opacity: 1, duration: 0.5, ease: "power2.out" }, 5.6)
        .to("#stat-label",     { opacity: 1, duration: 0.5 }, 6.2)
        .to("#stat-detail",    { opacity: 1, duration: 0.5 }, 6.8);

      /* Scene 3: CTA (t = 10–14s) */
      tl.to("#cta-badge",      { opacity: 1, duration: 0.35 }, 10.3)
        .to("#cta-headline",   { opacity: 1, duration: 0.6, ease: "power2.out" }, 10.6)
        .to("#cta-btn",        { opacity: 1, scale: 1, duration: 0.5, ease: "back.out(1.5)" }, 11.4)
        .to("#cta-link",       { opacity: 1, duration: 0.4 }, 11.9)
        .to(".nwm-logo-cta",   { opacity: 1, duration: 0.4 }, 12.2);

      window.__timelines["main"] = tl;
    </script>
  </body>
</html>
