#!/bin/bash
# ==== PAUSED FOR GIT HISTORY REWRITE — 2026-05-18 ====
# The auto-save daemon is intentionally disabled during a one-time
# git filter-repo history purge (27 GB .git bloat remediation).
# The original script is preserved at:
#   D:\Documents\nwm-history-rewrite-2026-05-18\backup.sh.orig
# and is restored at the end of the rewrite. Do NOT git add/commit/push
# from here while paused — it would race the rewrite.
echo "[backup] PAUSED for history rewrite maintenance ($(date '+%Y-%m-%d %H:%M')) — no-op, exiting 0"
exit 0
