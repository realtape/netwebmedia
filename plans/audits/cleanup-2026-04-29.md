# Cleanup Sweep — 2026-04-29

Owner: engineering-lead
Scope: 3 small, high-value fixes flagged in the final site rating audit.

---

## Task 1 — Footer emoji social glyphs swept site-wide

**Why:** BRAND.md "We Are Not Cute" rule. Final audit flagged 📸 ▶ 🎵 in footers — same violation as nav emojis swept earlier today.

**Icons added to `css/styles.css`** (mask-image SVG pattern, identical to the 17 industry icons already in the file, single-color via `currentColor`):

- `.icon-instagram`
- `.icon-youtube`
- `.icon-tiktok`
- `.icon-facebook`
- `.icon-linkedin` (added for future use; not yet in any footer)
- `.icon-x` (Twitter/X, future use)

Plus a footer-specific sizing override `.footer-social .social-link .icon { width:18px; height:18px }` so the SVG fills the existing 40x40 chip cleanly.

**Pages touched (footer block):**

| Page | Before | After |
|---|---|---|
| `index.html` | 📸 ▶ 🎵 + literal `f` | `.icon-instagram` `.icon-youtube` `.icon-tiktok` `.icon-facebook` |
| `services.html` | 📸 ▶ | `.icon-instagram` `.icon-youtube` |
| `about.html` | 📸 ▶ | `.icon-instagram` `.icon-youtube` |
| `faq.html` | `&#x1F4F8;` ▶ `&#x1F3B5;` `f` | `.icon-instagram` `.icon-youtube` `.icon-tiktok` `.icon-facebook` |

**Pages also touched (NWM-LIFT-TO-100 secondary social row, single trailing ▶):** `services.html`, `pricing.html`, `about.html`, `blog.html`. Replaced the lone ▶ with the text "YouTube" to match the surrounding pattern (Instagram / Facebook / TikTok in that block were already plain text — the ▶ was the odd one out).

**Pages checked, no change needed:** `contact.html` (footer-social block is empty), `compare.html` (no emoji or social-link markup found), `aeo-methodology.html` `case-studies.html` `aeo-survey.html` (per constraint — other agent owns).

**Schema.org `sameAs` FB ID `61573687500626` preserved** in `index.html:93`. Verified post-edit.

**Out of scope but noted:** `industries/local-services/photography/index.html:189` still has 📸 in an eyebrow string ("📸 Free growth plan — for Photography"). That is a hero-area decoration, not a footer social link. Same `site-upload/` folder has emoji social links across 4 files but it is a legacy backup directory not in the deploy workflow paths. Both can be swept in a follow-up PR.

---

## Task 2 — Company-pages price sweep

**Reality check vs. brief:** The brief said the live string is `Scale $1,999/mo` but the generator at `_deploy/generate_company_pages.py:189` actually emits `$1,999/mo Scale` (a different block) and contains zero references to "Free Audit". The 3,430 generated pages get their `Scale $1,999/mo` string from two **other** generators: `_deploy/generate_usa_audits.py` and `_deploy/generate_deep_audits.py`. Brief was directionally right; specifics were off.

**Generator templates fixed (3 files):**

- `_deploy/generate_company_pages.py` line 189 — `$1,999/mo Scale` → `$2,499/mo Scale`
- `_deploy/generate_usa_audits.py` lines 235–236 — `Scale $1,999/mo` and `Scale $1,999/mes` → `$2,499`
- `_deploy/generate_deep_audits.py` lines 786–787 — same two strings → `$2,499`

**In-place sweep of the 3,430 already-generated pages:** rather than regenerate (which depends on source HTML reports + classifiers and risks subtle content drift), I ran a `str.replace` script across `_deploy/companies/**/*.html` for the two literal strings:

- `Scale $1,999/mo` → `Scale $2,499/mo`
- `Scale $1,999/mes` → `Scale $2,499/mes`

**Result:** 3,430 files updated, 6,860 total substitutions (2 per file, en + es).

**Spot-check (post-sweep):**

- `_deploy/companies/antofagasta/amares-bistro.html:380-381` — both ES and EN now show `Scale $2,499/mes` and `Scale $2,499/mo`. Confirmed.
- `_deploy/companies/chillan/brbaros-barbershop.html:380-381` — same. Confirmed.
- Repo-wide grep for `Scale \$1,?999` in `_deploy/companies/` — zero matches.

**"Free Audit" CTA:** generators contain no occurrences of "Free Audit" or "Get a Free Audit" — nothing to sweep there. Skipped per scope.

---

## Task 3 — IndexNow ping fired

**Key file confirmed:** `97d9ba08245bd14f97cdfe7379896998.txt` at repo root, single line content `97d9ba08245bd14f97cdfe7379896998` (matches the keyLocation referenced in `.github/workflows/indexnow-ping.yml`).

**Curl run:**

```bash
curl -sS -o /tmp/indexnow_response.txt -w "HTTP %{http_code}\n" \
  -X POST "https://api.indexnow.org/indexnow" \
  -H "Content-Type: application/json; charset=utf-8" \
  --data '{"host":"netwebmedia.com","key":"97d9ba08245bd14f97cdfe7379896998","keyLocation":"https://netwebmedia.com/97d9ba08245bd14f97cdfe7379896998.txt","urlList":["https://netwebmedia.com/aeo-methodology.html","https://netwebmedia.com/case-studies.html","https://netwebmedia.com/aeo-survey.html","https://netwebmedia.com/sitemap.xml"]}'
```

**Response:** `HTTP 200` (empty body — normal for IndexNow accept). 200/202 are documented as accepted.

**Expected indexation latency:** Bing typically crawls within 5–30 minutes of an accepted IndexNow ping; Yandex/Seznam/Naver/Yep within hours. Google does not consume IndexNow — it relies on the GSC submitted-URL feed and natural crawl. The auto workflow `.github/workflows/indexnow-ping.yml` will fire again on the next site-root deploy, re-pinging the full sitemap.

---

## Anything noticed but not fixed today

1. **`industries/local-services/photography/index.html`** still ships `📸 Free growth plan — for Photography` in a hero eyebrow. Decorative emoji, not a footer issue, but same BRAND.md rule. Worth a follow-up.
2. **`site-upload/` directory** has 4 files (`index/about/blog/contact/services.html`) with emoji social links. Looks like an old uploaded snapshot. Not in any workflow's deploy path so it's not shipping — but it should either be deleted or re-synced before someone mistakes it for canonical.
3. **Two near-duplicate `Scale $1,999`-emitting generators** (`generate_usa_audits.py` and `generate_deep_audits.py`) plus a third with a different layout (`generate_company_pages.py`) is an obvious DRY violation. Worth consolidating to a single price-strings module so the next price change is a one-line fix, not a three-file sweep.
4. Schema.org `sameAs` arrays differ between pages — homepage has Instagram + Facebook only, while social channels in the footer also include YouTube/TikTok. Audit-target alignment opportunity for AEO.

No commits. All changes additive.
