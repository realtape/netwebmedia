# Instagram publisher dependencies for ig_publish.py
# Install with:  pip install -r scripts/ig_publish/requirements.txt
#
# Pinning instagrapi version because Meta breaks the unofficial API regularly —
# upgrading requires re-testing the publish flow. As of 2026-05-11 this version
# was confirmed working with carousel + reel uploads on US/CL accounts.
instagrapi==2.1.3

# Pillow is needed by instagrapi for image preflight (color space, size checks).
Pillow>=10.0.0
